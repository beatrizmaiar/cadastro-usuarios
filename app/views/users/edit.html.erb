<h1>Editar Usuário</h1>
<%= form_for @user do |f| %>
  <%= f.label :name, "Nome" %>
  <%= f.text_field :name %>

  <%= f.label :email, "Email" %>
  <%= f.text_field :email %>

  <%= f.submit "Salvar" %>
<% end %>